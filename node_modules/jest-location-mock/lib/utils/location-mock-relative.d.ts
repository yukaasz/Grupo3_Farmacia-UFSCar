import { LocationMock } from "@jedmao/location";
export declare class LocationMockRelative extends LocationMock implements Location {
    assign(url: string): void;
    replace(url: string): void;
    private makeAbsolute;
}
